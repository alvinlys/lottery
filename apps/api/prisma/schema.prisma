// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model Company {
    id      Int      @id @default(autoincrement())
    code    String   @unique
    name    String   @unique
    results Result[]

    @@map("companies")
}

model Result {
    id         Int      @id @default(autoincrement())
    companyId  Int
    company    Company  @relation(fields: [companyId], references: [id])
    date       DateTime @db.Date
    drawNumber String   @map("draw_number") // 5746/24
    result     String[] // prize (3), special (10), consolation (10)

    @@map("results")
}
